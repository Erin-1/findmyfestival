<%= @review.content %>
<%= @review.stars %>

<%= link_to "Edit review", edit_review_path(@review),class: "btn btn-info" %>

<%= link_to "Delete review", review_path(@review), method: :delete, data: {confirm: "You sure??" }, class: "btn btn-info" %>
