<div class="banner2">
  <div class="banner2-content">
<div class="container">
  <div class="row">
    <% @festivals.each do |festival| %>
      <div class="col-xs-4">
        <div class="card">
        <h2><%= festival.title %></h2>
        <p><%= festival.description %></p>
        <p><%= festival.address %></p>
        <!-- festival.photo #scrape, parse to seed database then interpolate photo from hash??? -->

        <%= link_to "More Info", festival_path(festival), class: "btn btn-info" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
</div>
</div>

<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
></div>

<%= link_to "Add a festival", new_festival_path, class: "btn btn-info" %>

<%= link_to "Home", root_path, class: "btn btn-info" %>
