<div class="banner2">
  <div class="banner2-content">
    <div class="container">
      <div class="row">
        <% @festivals.each do |festival| %>
        <div class="col-xs-4">
          <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)),
          url('<%= image_path festival.photo, height: 300, width: 400 %>')">
          <div class="card-category"><%= festival.title %></div>
          <!-- <div class="card-description"><%= festival.description %></div> -->
          <!-- festival.photo #scrape, parse to seed database then interpolate photo from hash??? -->

          <%= link_to "More Info", festival_path(festival), class: "button_card" %>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
</div>

<div
id="map"
style="width: 100%;
height: 600px;"
data-markers="<%= @markers.to_json %>"
></div>

<% if current_user.host? %>
<%= link_to "Add a festival", new_festival_path, class: "btn btn-info" %>
<%= link_to "My guests", dashboards_my_guests_path, class: "btn btn-info" %>
<% end %>
<!--
 link_to "Home", root_path, class: "button" %>
cl_image_tag("q6yvvmkxllq2szqjjee7",
width: 400, height: 300, crop: :fill)%> -->
